<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App1.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>        
        <StackLayout Margin="0,50,0,0" Padding="0,0,0,0" VerticalOptions="Start">
            <StackLayout Margin="0,0,0,30" BackgroundColor="#2196F3" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
                <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                    <ContentView Padding="0,20,0,20" VerticalOptions="FillAndExpand">
                        <Image Source="ventAppLogo.png" VerticalOptions="Center" HeightRequest="124" />
                    </ContentView>
                </StackLayout>
            </StackLayout>
            <Label Text="Usuario:" FontSize="Subtitle" />
            <Entry FontSize="Subtitle" />
            <Label Text="Clave:" FontSize="Subtitle" />
            <Entry FontSize="Subtitle" />
            <Button Margin="10,30,10,0" Padding="0,0,0,0" VerticalOptions="Center" Text="Ingresar" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>